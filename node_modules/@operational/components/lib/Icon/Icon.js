"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0)
            t[p[i]] = s[p[i]];
    return t;
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const React = __importStar(require("react"));
const styled_1 = __importDefault(require("../utils/styled"));
const constants_1 = __importStar(require("../utils/constants"));
const Icon_Brand_1 = __importDefault(require("./Icon.Brand"));
const CustomIcons = __importStar(require("./Icon.Custom"));
const Icon = (_a) => {
    var { left, right, color, name } = _a, props = __rest(_a, ["left", "right", "color", "name"]);
    const iconColor = constants_1.expandColor(constants_1.default, color) || "currentColor";
    const TypedCustomIcons = CustomIcons;
    if (TypedCustomIcons[name]) {
        const Comp = TypedCustomIcons[name];
        return React.createElement(Comp, Object.assign({}, props, { size: props.size || 18, color: iconColor }));
    }
    if (Icon_Brand_1.default[name]) {
        const Comp = Icon_Brand_1.default[name];
        return React.createElement(Comp, Object.assign({}, props, { size: props.size || 32, color: iconColor }));
    }
    return null;
};
Icon.defaultProps = {
    size: 18,
};
exports.default = styled_1.default(Icon)(({ left, right, theme, onClick }) => ({
    marginLeft: right ? theme.space.small : 0,
    marginRight: left ? theme.space.small : 0,
    cursor: Boolean(onClick) ? "pointer" : "default",
    transition: "fill .075s ease",
}));
//# sourceMappingURL=Icon.js.map