"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const path_1 = require("path");
const BLACK_LIST = ["Internals", "utils", "__tests__", "polyfills.ts", "index.ts", "types.ts", ".DS_Store"];
describe("lib exports", () => {
    const mainExport = fs_1.readFileSync(path_1.join(__dirname, "../index.ts"), "utf-8");
    fs_1.readdirSync(path_1.join(__dirname, "../"))
        .filter(name => !BLACK_LIST.includes(name))
        .forEach(name => {
        it(`should export ${name} component into index.ts`, () => {
            expect(mainExport.includes(name)).toBe(true);
        });
    });
});
//# sourceMappingURL=libExports.js.map