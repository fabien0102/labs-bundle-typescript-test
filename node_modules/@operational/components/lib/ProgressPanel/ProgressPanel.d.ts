import * as React from "react";
import { DefaultProps } from "../types";
export declare type Status = "waiting" | "todo" | "running" | "success" | "failure" | "done" | "failed";
export interface ProgressPanelProps extends DefaultProps {
    /** Progress items */
    items: Array<{
        /** Progress item status */
        status: Status;
        /** Progress item title */
        title: string;
        /** Progress item error, if applicable */
        error?: string;
    }>;
}
declare const ProgressPanel: React.SFC<ProgressPanelProps>;
export default ProgressPanel;
