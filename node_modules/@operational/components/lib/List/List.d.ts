import * as React from "react";
export declare type ImageURL = string;
export interface ListProps {
    items: Array<{
        title?: string;
        photo: ImageURL | React.ReactNode;
        description: string;
        onClick?: () => void;
    }>;
    fullWidth?: boolean;
}
declare const List: React.SFC<ListProps>;
export default List;
