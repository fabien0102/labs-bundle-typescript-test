import * as React from "react";
export interface WindowSize {
    width: number;
    height: number;
}
export declare type MessageType = "info" | "success" | "error";
export interface IMessage {
    body: string;
    type: MessageType;
}
export interface Context {
    pushState?: (url: string) => void;
    replaceState?: (url: string) => void;
    pushMessage: (message: IMessage) => void;
    windowSize: WindowSize;
    loading: boolean;
    setLoading: (isLoading: boolean) => void;
}
declare const Provider: React.ComponentType<React.ProviderProps<Context>>, OperationalContext: React.ComponentType<React.ConsumerProps<Context>>;
export default OperationalContext;
export { Provider };
