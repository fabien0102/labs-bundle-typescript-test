import * as React from "react";
import { DefaultProps } from "../types";
export interface DatePickerProps extends DefaultProps {
    id?: string;
    label?: string;
    /** Min date in the format YYYY-MM-DD. Dates lower than this cannot be selected. */
    min?: string;
    /** Max date in the format YYYY-MM-DD. Dates higher than this cannot be selected. */
    max?: string;
    /** Start date in the format YYYY-MM-DD. */
    start?: string;
    /** End date in the format YYYY-MM-DD. */
    end?: string;
    /** Triggered every time the start or end dates change. `undefined` values clear start or end values. */
    onChange?: (date: {
        start?: string;
        end?: string;
    }) => void;
    /** Placeholder text when no dates selected */
    placeholder?: string;
}
export interface State {
    isExpanded: boolean;
    year: number;
    /** Current month. Starting at 0, corresponding to January */
    month: number;
}
declare class DatePicker extends React.Component<DatePickerProps, State> {
    static defaultProps: {
        placeholder: string;
    };
    constructor(props: DatePickerProps);
    containerNode: any;
    inputNode: any;
    keypressHandler?: EventListenerOrEventListenerObject;
    outsideClickHandler?: EventListenerOrEventListenerObject;
    validate(props?: DatePickerProps): void;
    changeMonth(diff: number): void;
    componentDidMount(): void;
    componentDidUpdate(): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
}
export default DatePicker;
