import * as React from "react";
import { DefaultProps } from "../types";
declare type ResizeOptions = "none" | "both" | "vertical" | "horizontal";
export interface TextareaProps extends DefaultProps {
    /** Controlled value of the field */
    value: string;
    /** Label of the field */
    label?: string;
    /** OnChange handler */
    onChange?: (val: string) => void;
    /** Change the font to monospace to better display of code */
    code?: boolean;
    /** Custom starting height */
    height?: number;
    /** Text for a hint */
    hint?: string;
    /** Error text */
    error?: string;
    /** Should the input fill its container? */
    fullWidth?: boolean;
    /** Is it disabled? */
    disabled?: boolean;
    /** Action button/link - right of header */
    action?: React.ReactNode;
    /** Resize option */
    resize?: ResizeOptions;
    /** Copy text to clipboard on click */
    copy?: boolean;
    /** cmd+enter submit handler */
    onSubmit?: () => void;
}
export interface State {
    showTooltip: boolean;
}
declare class Textarea extends React.Component<TextareaProps, State> {
    timeoutId: number | null;
    state: {
        showTooltip: boolean;
    };
    static defaultProps: Partial<TextareaProps>;
    showTooltip: () => void;
    render(): JSX.Element;
}
export default Textarea;
