"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const React = __importStar(require("react"));
const styled_1 = __importDefault(require("../utils/styled"));
const utils_1 = require("../utils");
const color_1 = require("../utils/color");
const constants_1 = require("../utils/constants");
const Container = styled_1.default("div")(({ theme, color, left, right, children, assignColor }) => {
    const backgroundColor = assignColor
        ? color_1.colorMapper(theme.color.palette)(children)
        : constants_1.expandColor(theme, color) || theme.color.primary;
    const textColor = utils_1.readableTextColor(backgroundColor, [theme.color.white, theme.color.black]);
    return Object.assign({ backgroundColor, color: textColor, fontSize: theme.font.size.fineprint, fontWeight: theme.font.weight.bold, width: 18, height: 18, flexShrink: 0, borderRadius: theme.borderRadius, display: "inline-flex", alignItems: "center", justifyContent: "center" }, (left ? { marginRight: theme.space.small } : {}), (right ? { marginLeft: theme.space.small } : {}));
});
const SmallNameTag = props => (React.createElement(Container, Object.assign({}, props, { assignColor: Boolean(!props.color) }), props.children || ""));
SmallNameTag.defaultProps = {
    children: "",
};
exports.default = SmallNameTag;
//# sourceMappingURL=SmallNameTag.js.map