"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0)
            t[p[i]] = s[p[i]];
    return t;
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const React = __importStar(require("react"));
const styled_1 = __importDefault(require("../utils/styled"));
const utils_1 = require("../utils");
const color_1 = require("../utils/color");
const constants_1 = require("../utils/constants");
const Container = styled_1.default("div")(({ theme, color_, left, right, children, assignColor }) => {
    const backgroundColor = assignColor
        ? color_1.colorMapper(theme.color.palette)(children)
        : constants_1.expandColor(theme, color_) || theme.color.primary;
    const textColor = utils_1.readableTextColor(backgroundColor, [theme.color.white, theme.color.black]);
    return Object.assign({ backgroundColor, color: textColor, fontSize: theme.font.size.small, fontWeight: theme.font.weight.bold, width: 28, height: 20, borderRadius: theme.borderRadius, display: "inline-flex", alignItems: "center", justifyContent: "center" }, (left ? { marginRight: theme.space.small } : {}), (right ? { marginLeft: theme.space.small } : {}));
});
const NameTag = (_a) => {
    var { color, children } = _a, props = __rest(_a, ["color", "children"]);
    return (React.createElement(Container, Object.assign({}, props, { color_: color, assignColor: Boolean(!color) }), children || ""));
};
NameTag.defaultProps = {
    children: "",
};
exports.default = NameTag;
//# sourceMappingURL=NameTag.js.map