import * as React from "react";
import { DefaultProps } from "../types";
export interface DeprecatedProps extends DefaultProps {
    running?: boolean;
    success?: boolean;
    error?: boolean;
    state?: never;
}
export interface LatestProps extends DefaultProps {
    running?: never;
    success?: never;
    error?: never;
    state?: "error" | "success" | "running" | "neutral";
}
export declare type StatusProps = DeprecatedProps | LatestProps;
declare const Status: React.SFC<StatusProps>;
export default Status;
